
function main(args) {
	if (!${args}){
		var json = ${args.page}!;
		var title = JSON.parse(title);
		var date = Date.now();
		return { title: title, date: date}

	}else{
		return { title: `null`, date: `null}
	}
}



